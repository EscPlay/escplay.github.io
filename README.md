# EscPlay Website

Static placeholder website for EscPlay.

## Local Preview

Open `index.html` directly in a browser. No build step is required.

## Deployment

Pushing to `master` runs the Pages workflow and publishes the repository root as the static site artifact.

The custom domain should be configured in the host settings, not through a `CNAME` file, because this site deploys through the workflow.
